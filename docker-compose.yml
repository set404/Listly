services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: listly
      POSTGRES_PASSWORD: listly
      POSTGRES_DB: listly
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U listly"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
